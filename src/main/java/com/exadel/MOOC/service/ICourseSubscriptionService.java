package com.exadel.MOOC.service;

public interface ICourseSubscriptionService {

    boolean subscribe(Long userId, Long courseId);

//    boolean isSubscribed (Long userId, Long courseId);

//    boolean unsubscribe(Long userId, Long courseId);
}
